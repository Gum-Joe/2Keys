import { expect } from "chai";
import { TWOKEYS_MAIN_CONFIG_DEFAULT_PATH } from "../lib/constants.js";


describe("Misc tests", () => {
	it("should create the app data folder when process.env.APPDATA is not present");
});
