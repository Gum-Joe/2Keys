language: node_js
node_js:
  - lts/*
  - node

os: windows

# Default is fine here
before_install:
  # Convery lock file
  - cd server
  - choco install yarn
install:
  - powershell -Command refreshenv; yarn
script:
  - npm run coverage
  - npx codecov